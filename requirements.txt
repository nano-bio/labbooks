Django==3.2.12
gunicorn==20.1.0
mysqlclient==2.1.0 # install this with --no-cache-dir --upgrade
crispy-bootstrap5==0.6

# for surftof
djangorestframework==3.13.1

# for mass-spectra
scipy==1.5.4 # pip complains on server that a newer version does not exists
h5py==2.10.0 # v3 doesnt work on windows

# for journal
django-crispy-forms==1.14.0
django-ckeditor==6.2.0
matplotlib==3.3.4 # pip complains on server that a newer version does not exists

# for cheminventory
requests==2.27.1
openpyxl==3.0.9
periodictable==1.6.0
# fitlib==0.0.* # py -m pip install git+https://github.com/nano-bio/fitlib.git@package

# for snowball & surftof
numpy==1.21.0 # pip complains on server that a newer version does not exists

# for wippi
pytz==2021.3

# for nanoparticles
NSFopen==1.1.13
pandas==1.3.5
